services:
  ai:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - embedding-cache:/root/.cache
    command: uv run uvicorn main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  embedding-cache:
